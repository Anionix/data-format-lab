{
  "schema_version": "1",
  "name": "fastlanes",
  "repository": "https://github.com/cwida/FastLanes",
  "source_commits": {"core": "f0edc1020a538f1f8098640fce8347c9ac247a0d"},
  "primary_source": "https://fastlanes.io/",
  "claim": "nested encodings designed for parallel SIMD-friendly decoding",
  "attempted_on": "2026-07-14",
  "comparability": "PARTIAL",
  "state": "FAILED",
  "claim_summary": "numeric data round-tripped; partial-vector string dictionary lookup failed; Python binding rebuild blocked before mixed-schema test",
  "correction": "the earlier numeric -11 result used comma-separated input against the official pipe-delimited reader and is superseded",
  "attempts": [
    {
      "entrypoint": "official pipe-delimited numeric conversion",
      "workload": "1,000,000 rows and 8 numeric columns",
      "result": "round-trip succeeded"
    },
    {
      "entrypoint": "official pipe-delimited string conversion",
      "workload": "string dictionary at 1,023, 1,024, 1,025, 2,048, and 2,049 rows",
      "result": "1,024 and 2,048 succeeded; partial vectors failed dictionary lookup"
    },
    {
      "entrypoint": "official reader with invalid comma-delimited input",
      "workload": "malformed delimiter input",
      "result": "process exited -11; retained as robustness evidence"
    },
    {
      "entrypoint": "official pyfastlanes editable build with scikit-build-core",
      "attempted_on": "2026-07-17",
      "workload": "build the Python extension before a 13-column mixed-schema round trip",
      "environment": {
        "host": "Darwin esunoMacBook-Pro 27.0.0 arm64",
        "macos": "27.0 (26A5378j)",
        "sdk_version": "27.0",
        "sdk_path": "/Library/Developer/CommandLineTools/SDKs/MacOSX.sdk",
        "architecture": "arm64",
        "python": "3.12.13",
        "clang": "21.1.8",
        "scikit_build_core": "0.11.6",
        "cmake": "4.4.0",
        "ninja": "1.13.0",
        "pybind11": "2.12.1",
        "setuptools_scm": "8.3.1",
        "build_type": "Release",
        "parallelism": 4,
        "cmake_args": [
          "-DFLS_BUILD_PYTHON=ON",
          "-DFLS_ENABLE_VERBOSE_OUTPUT=ON"
        ]
      },
      "result": "build stopped at src/table/stats.cpp:17 for TypedStats<std::string> with -Werror,-Wnonnull; no mixed-schema round trip was attempted"
    }
  ],
  "retry_when": "the official Python extension builds in a reproducible toolchain, round-trips partial-vector string dictionaries, and rejects malformed input without terminating the process"
}
